deactivate 							# if you are inside of venv already
Remove-Item -Recurse -Force venv    # deletes venv

python -m venv venv                 # create new venv
venv/scripts/activate               # activate venv

python -m pip install --upgrade pip # update pip to latest version
pip install mkdocs mkdocs-material  # install mkdocs and mkdocs material
pip install mkdocs-git-revision-date-localized-plugin  # install for mkdocs gh deploy

$env:GIT_PYTHON_GIT_EXECUTABLE = "C:\Program Files\Git\cmd\git.exe" # set git executable path for gitpython

mkdocs --version                    # verify version
mkdocs serve                        # serve mkdocs locally (test at "http://127.0.0.1:8000/" to see if it works)

mkdocs gh-deploy                    # deploy to git